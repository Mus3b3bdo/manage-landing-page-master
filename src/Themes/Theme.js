import { createTheme } from "@mui/material/styles";

export const myTheme = createTheme({
  palette: {
    primary: {
      main: "hsl(12, 88%, 59%)",
    },
    secondary: {
      main: "hsl(228, 39%, 23%)",
    },
  },
});
